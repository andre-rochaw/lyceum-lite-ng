export const environment = {
  production: false,
  /** Proxy: /api ? http://localhost:8080 (proxy.conf.json). */
  apiUrl: '/api',
  /** Padrao dos testes manuais: sempre API real em localhost:8080. */
  useMockAuth: false,
};
